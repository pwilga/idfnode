## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  cikon_config:
    git: "https://github.com/pwilga/cikon-iot-solution.git"
    path: components/cikon_config
  cikon_core:
    git: "git@github.com:pwilga/cikon-iot-solution.git"
    path: components/cikon_core
    # path: ../../cikon-iot-solution/components/cikon_core
  cikon_mqtt:
    git: "https://github.com/pwilga/cikon-iot-solution.git"
    path: components/cikon_mqtt
  cikon_wifi:
    git: "git@github.com:pwilga/cikon-iot-solution.git"
    path: components/cikon_wifi
    # path: ../../cikon-iot-solution/components/cikon_wifi
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  espressif/mdns: '*'
  espressif/button: '*'
